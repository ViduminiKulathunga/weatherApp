class Cities {
  constructor(id, CityCode, CityName, Temp, Status) {
    this.id = id;
    this.CityCode = CityCode;
    this.CityName = CityName;
    this.Temp = Temp;
    this.Status = Status;
  }
}

export default Cities;
